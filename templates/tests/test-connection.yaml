apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "go-latest.fullname" . }}-test-connection"
  labels:
    {{- include "go-latest.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "go-latest.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
